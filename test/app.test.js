const sayHello = require('./app').sayHello; const assert = require('assert'); const app = require('../app'); const should = require('chai').should(); describe('# App test! - original', function () { it('sayHello should return hello', function (done) { if (sayHello() === 'hello') { done(); } }); it('should greater than 5', function () { app.addNumbers(3, 4).should.be.above(5); }); describe('# Custom ', () => { it('Function axiosRequest test', (axiosRequest) => assert.equal(3, axiosRequest())); }) });
